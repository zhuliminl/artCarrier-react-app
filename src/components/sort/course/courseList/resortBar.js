/*
 * Created by Saul at 2018/05/22
 *
 * 重新排序模块
 * 按价格排序、按销量排序、按折扣排序等
 *
 */

import React from 'react';
import {
  View,
  Text,
  ScrollView,
  FlatListView,
  StyleSheet,
  Image
} from 'react-native';
import {
  Color, Font, Layout, Space
} from '../../../../constants';

import ToggleComposition from './toggleComposition';

// 暂停排序开发
class ResortBar extends React.Component {
  render() {
    return(
      <View>

      </View>
    )
  }
}

export default ResortBar;
